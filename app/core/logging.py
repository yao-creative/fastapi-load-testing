"""
Logging skeleton for Celery + Redis tutorial work.

When you implement the exercises, add shared logging helpers here instead of
spreading log-format logic across routes and tasks.
"""


# TODO: add structured logger setup if you want task_id, queue, and attempt
# TODO: add helpers so API logs and worker logs share the same correlation fields
# TODO: keep logging setup separate from task definitions
