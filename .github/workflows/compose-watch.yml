name: Compose Watch Smoke

on:
  push:
  pull_request:

jobs:
  compose-watch-smoke:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Run docker compose watch smoke check
        run: bash scripts/compose_watch_smoke.sh
